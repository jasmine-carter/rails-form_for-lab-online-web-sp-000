<h3> New Student Form </h3>
<%= form_for(@student) do |f| %>
<label> Class Title </label>
<%= f.text_field :title %>
<label> Room Number </label>
<%= f.text_field :room_number %>

<label>Create School class </label>
<%= f.submit %>
<% end %>
